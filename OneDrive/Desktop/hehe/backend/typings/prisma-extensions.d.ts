/**
 * Placeholder file — Prisma client type augmentations.
 *
 * The Prisma client needs to be regenerated after recent schema additions
 * (followerSnapshot, audienceDemographic, campaign models).
 *
 * Run: npm run prisma:generate
 *
 * Once regenerated, the missing-model TypeScript errors in analytics.service.ts
 * and campaigns.service.ts will resolve automatically.
 */

export {};
